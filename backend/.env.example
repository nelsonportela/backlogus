# Database
DATABASE_URL="postgresql://backlogus_user:backlogus_password@localhost:5432/backlogus_db"

# JWT Secret (generate a secure random string)
JWT_SECRET="your-super-secure-jwt-secret-here"

# Server Configuration
PORT=3001
NODE_ENV="development"

# Note: API credentials are now stored per-user in the database
# Users can add their own IGDB and TMDB credentials in the Settings page